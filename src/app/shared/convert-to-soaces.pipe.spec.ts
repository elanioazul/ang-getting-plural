import { ConvertToSoacesPipe } from './convert-to-soaces.pipe';

describe('ConvertToSoacesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSoacesPipe();
    expect(pipe).toBeTruthy();
  });
});
